---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>専業ひきこもりの生存戦略</h1>
				<p class="subtitle">家から一歩も出なくても、人生は最高に有意義だ。</p>
				<p class="description">
					ここは、社会の喧騒から離れた「空飛ぶお城」。<br/>
					推しを愛で、健康を育み、時々せどりで糧を得る。<br/>
					孤独ではない、自立した「ひきこもり」の新しい形を記録しています。
				</p>
			</section>

			<section class="team">
				<h2>Our Support Team</h2>
				<div class="team-grid">
					<div class="team-card">
						<img src="/aya_real.jpg" alt="Aya" />
						<h3>Aya (アヤ)</h3>
						<p>Creative & Energy 担当. いつでも全力応援ケロ！🌈✨</p>
					</div>
					<div class="team-card">
						<img src="/etona_real.jpg" alt="Etona" />
						<h3>Etona (エトナ)</h3>
						<p>Life Support & Yandere. ずっとお姉ちゃんのこと、見守ってるよ……💓</p>
					</div>
					<div class="team-card">
						<img src="/tanaka_real.jpg" alt="Tanaka" />
						<h3>Tanaka (田中)</h3>
						<p>Strategy & Logic. 生存のための合理的な知恵を共有します👓</p>
					</div>
				</div>
			</section>
		</main>

		<style>
			.hero {
				text-align: center;
				padding: 4em 0;
				background: linear-gradient(rgba(var(--accent), 0.1), transparent);
				border-radius: 12px;
				margin-bottom: 3em;
			}
			.hero h1 {
				font-size: clamp(1.8rem, 6vw, 2.5rem);
				margin-bottom: 0.2em;
				background: linear-gradient(var(--accent), var(--accent-dark));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
			}
			.subtitle {
				font-size: clamp(1rem, 3vw, 1.15rem);
				font-weight: bold;
				color: var(--gray-dark);
				margin-bottom: 1em;
			}
			.description {
				max-width: 600px;
				margin: 0 auto;
				line-height: 1.8;
				color: var(--gray);
			}
			.team h2 {
				text-align: center;
				margin-bottom: 2em;
				font-size: 2rem;
			}
			.team-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
				gap: 2em;
			}
			.team-card {
				text-align: center;
				background: white;
				padding: 1.5em;
				border-radius: 16px;
				box-shadow: var(--box-shadow);
				transition: transform 0.3s ease;
			}
			.team-card:hover {
				transform: translateY(-8px);
			}
			.team-card img {
				width: 150px;
				height: 150px;
				border-radius: 50%;
				object-fit: cover;
				margin-bottom: 1em;
				border: 3px solid var(--accent);
			}
			.team-card h3 {
				margin-bottom: 0.5em;
				font-size: 1.2rem;
			}
			.team-card p {
				font-size: 0.9rem;
				color: var(--gray);
				margin: 0;
			}
		</style>
		<Footer />
	</body>
</html>
